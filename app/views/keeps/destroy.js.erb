var $keepsCount = $("#keeps_count"),
    $actBtn     = $('.action_menu ul');

$keepsCount.attr('data-badge', "<%= @question.keeps_count %>");
$actBtn.html(
  '<%= j(render partial: 'keeps/act_btn') %>'
);
