var $homeTimeLine = $('#home-timeline');
$homeTimeLine.switchClass('questions-menu box-sizing', 'categories-menu');
$homeTimeLine.html(
  '<%= j(render partial: 'tags/tag', collection: @tags) %>'
);
$('.top-bar h2').text('タグ一覧');
